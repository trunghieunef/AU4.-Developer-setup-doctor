# Copy to .env, then replace the placeholder. Never commit .env.
SETUP_DOCTOR_API_KEY="your_api_key"
SETUP_DOCTOR_AI=true
# Optional: overrides [ai].model in setup-doctor.toml.
SETUP_DOCTOR_AI_MODEL="gpt-4o-mini"
